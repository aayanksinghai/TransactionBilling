CREATE DATABASE BILLING;

USE BILLING;

CREATE TABLE ACCOUNT (USERNAME varchar(30) PRIMARY KEY, PASSWORD varchar(30) NOT NULL);

INSERT INTO ACCOUNT VALUES ("admin","AALOK@19");

CREATE TABLE STOCKS (ITEM_NO INT auto_increment PRIMARY KEY, HSN_CODE varchar(100) NOT NULL, ITEM_NAME varchar(500) NOT NULL, TAX_SLAB Double NOT NULL);

CREATE TABLE USER (BILLNO varchar(50) PRIMARY KEY, NAME varchar(100) NOT NULL, CONTACT_NO varchar(30) NOT NULL, ADDRESS varchar(200) NOT NULL, GSTIN varchar(30), TOTAL_AMOUNT Double NOT NULL, BILL_DATE TIMESTAMP DEFAULT CURRENT_TIMESTAMP);

CREATE TABLE BILL (ID INT auto_increment PRIMARY KEY, BILLNO varchar(50) NOT NULL, HSN_CODE varchar(100) NOT NULL, ITEM_NAME varchar(500) NOT NULL, QUANTITY varchar(30) NOT NULL, TAX_SLAB Double NOT NULL, SELLING_PRICE Double NOT NULL,BASIC_PRICE Double NOT NULL, CGST Double NOT NULL, SGST Double NOT NULL, TOTAL_PRICE Double NOT NULL, foreign key(BILLNO) references USER(BILLNO));

LOAD DATA LOCAL INFILE 'C:/Users/HP/Documents/NetBeansProjects/TransactionBilling/Stocks.csv' INTO TABLE STOCKS_DUMMY FIELDS TERMINATED BY ',' ENCLOSED BY '"' LINES TERMINATED BY '\r\n' IGNORE 1 LINES (@col1,  @col2, @col3) SET HSN_CODE = @col1, ITEM_NAME = @col2, TAX_SLAB = @col3;

drop database billing;

ALTER TABLE STOCKS MODIFY HSN_CODE VARCHAR(100);
ALTER TABLE STOCKS MODIFY ITEM_NAME VARCHAR(500);

ALTER TABLE USER MODIFY BILLNO VARCHAR(50);
ALTER TABLE USER MODIFY NAME VARCHAR(100);
ALTER TABLE USER MODIFY ADDRESS VARCHAR(200);

ALTER TABLE BILL MODIFY BILLNO VARCHAR(50);
ALTER TABLE BILL MODIFY HSN_CODE VARCHAR(100);
ALTER TABLE BILL MODIFY ITEM_NAME VARCHAR(500);
 



